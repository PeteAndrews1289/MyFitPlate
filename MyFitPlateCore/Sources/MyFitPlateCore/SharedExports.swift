// The widget and Live Activity extensions link only MyFitPlateShared, so they don't carry a
// copy of Core. Core re-exports it, so the app and Core see those types without an import.
@_exported import MyFitPlateShared
